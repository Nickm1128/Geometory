# Autonomous Milestone Run

Use this prompt to resume substantial work without relying on prior conversation context:

> Continue Geometory Milestone 1 autonomously from repository state. Follow `AGENTS.md` and the Geometory open-work workflow, resume the current task in `docs/open_work/INDEX.md`, complete dependency-safe work in order, update checkboxes and evidence immediately, record blockers and continue eligible parallel work, run every required validation and hygiene gate, and create only the permitted Git checkpoints. Stop only when Milestone 1 is complete or every eligible lane requires my input.

Before using the prompt, ensure the working tree contains no unexplained edits and the intended coordinator is the only agent authorized to update trackers or publish Git state.
