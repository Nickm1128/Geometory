# Autonomous Milestone Run

Use this prompt to resume substantial work without relying on prior conversation context:

> Continue Geometory Milestone 1 autonomously from repository state. Follow `AGENTS.md` and the Geometory open-work workflow, resume the current task in `docs/open_work/INDEX.md`, complete dependency-safe work in order, update checkboxes and evidence immediately, record blockers and continue eligible parallel work, run every required validation and hygiene gate, and create only the permitted Git checkpoints. Stop only when Milestone 1 is complete or every eligible lane requires my input.

Before using the prompt, ensure the working tree contains no unexplained edits and the intended coordinator is the only agent authorized to update trackers or publish Git state.

## Fresh GPT-5.6 Terra / Think High Thread

Select **GPT-5.6 Terra** and **Think: High** in the new thread before sending
this prompt. Model/reasoning selection is a UI setting; the prompt does not
change it by itself.

> Continue Geometory Milestone 1 autonomously in `C:\Users\milin\Documents\Geometory`. This prompt is my explicit acknowledgement of the completed P00 report boundary and authorization to begin P01 and continue through the remaining approved M1 plan without waiting between phases. Rebuild context from repository state: read `AGENTS.md`; verify canonical skill synchronization; use the Geometory project-context, open-work, validation, visual-QA, and bot-training skills when their scopes apply; read `docs/open_work/INDEX.md`, `MILESTONE_1_PLAN.md`, and the active phase's four files; run the Resume audit; fetch without pulling; and inspect branch, log, status, divergence, and task-relevant files. The repository is authoritative if it differs from this prompt. Expected handoff is clean/synchronized `milestone/m1-vertical-slice`, completed immutable tag `m1-p00`, active `M1-P01-T01`, and no open blocker.
>
> Execute dependency-safe tasks in order, starting with contract/authority reconciliation and focused failing tests before P01 implementation. Work one task through implementation, required validation, evidence, documentation, and its atomic task-ID commit before switching. Update task/gate checkboxes, phase notes, run log, blocker state, and the exact next action immediately at every boundary. Use fresh bounded subagents for independent audits or disjoint work when useful, but keep one coordinating agent exclusively responsible for trackers, integration, Git commits/tags/pushes, bot promotion, and paid calls. Make reversible authority-backed choices without asking routine questions; record durable decisions in `docs/ASSUMPTIONS.md`. Inspect real CI logs before fixing failures and never weaken a gate merely to obtain green status.
>
> Complete every required validation, Android/visual/bot evidence set, phase hygiene pass, immutable phase tag, remote verification, and transition through P06. Keep P2P, lobbies, accounts, servers, additional maps/players/units, runtime LLMs, and other documented deferrals out of M1. Never pull through divergence, reset unknown work, force-push, move a tag, merge to `main`, expose a device identifier/secret, or place an API key/model runtime in the APK. The approved P04 live cycle remains limited by its dedicated capped credential, privacy rules, one-request-per-cycle rule, and recorded microdollar budgets; missing/uncapped credentials or ambiguous spend block that lane, while only explicitly eligible parallel work continues. Do not substitute mocked evidence for a live gate or emulator evidence for a required physical-device gate.
>
> Continue until Milestone 1 is genuinely complete and remotely verified, or until every dependency-safe lane requires my input or an external state change. When one lane blocks, record it and continue all eligible work; batch the remaining questions for me. Do not stop merely to summarize a phase, because this prompt clears the prior report-only pause. Begin executing the repository's exact current task now rather than returning only a plan.
