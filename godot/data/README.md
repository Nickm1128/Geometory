# Godot Runtime Data

This folder contains the data files Godot can load through `res://data` and include in exports.

Source-of-truth data lives at the repository root under `data/`. Run `tools/sync_godot_data.ps1` after changing root data files.
